<Page
    x:Class="RestaurantManager.UniversalWindows.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager.UniversalWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <AppBar Grid.ColumnSpan="3" Background="#FF1F1F1F" Foreground="White" 
                ClosedDisplayMode="Minimal" Opened="AppBar_Opened" Margin="0,-1,0,0">
            <AppBarButton x:Name="barBtnHome" Icon="Home" Label="Home" 
                          Foreground="White" Click="barBtnHome_Click" >

            </AppBarButton>
        </AppBar>

        <ListView x:Name="listView" Margin="0" Background="#FF832727" Height="1069" />

        <Button x:Name="btBlkClearAll" Content="Clear All Order"
                Grid.Column="0" Grid.Row="4" HorizontalAlignment="Center" 
                VerticalAlignment="Center" FontSize="64" />

    </Grid>
</Page>
